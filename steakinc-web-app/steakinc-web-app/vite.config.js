import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: '/elee1149-coursework--steak/', // 🔥 Change this to match your repo name
});
